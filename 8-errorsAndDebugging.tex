\documentclass{article}               % Defines the document class as 'article'
\usepackage{amsmath}                 % Loads AMS Math package for advanced math features

\begin{document}
	
	% $$\left( \frac{x}{2} - x ) = 0$$			% incorrect: mismatched \left( with no \right)
	$$\left( \frac{x}{2} - x \right) = 0$$		% corrected: properly matched parentheses with \left and \right
	
	% $$x = \frac{b^2-4ac){2a}$$  				% incorrect: unmatched braces and wrong delimiter
		$$x = \frac{b^2-4ac}{2a}$$					% corrected: proper fraction syntax
		
		% $$t = \sqrt{\frac{x-z}{a}$$				% incorrect: missing closing braces for \sqrt
			$$t = \sqrt{\frac{x-z}{a}}$$				% corrected: square root and fraction properly enclosed
			
			\begin{enumerate}                           % begin numbered list
				\item First                             % first item in list
				\item Second                            % second item in list
				\begin{enumerate}                       % nested enumerate list
					\item Sub1                          % first sub-item
					\item Sub2                          % second sub-item
					\item Third                         % third sub-item (nested)
				\end{enumerate}
			\end{enumerate}
			
			% $$\farc{3}{4}$$				% incorrect: 'farc' is not a valid LaTeX command
			$$\frac{3}{4}$$                  % correct fraction using \frac
			
			% The discriminant of a quadratic is $b^2 - 4ac.  If that discriminant				% incorrect: math mode not properly closed
			The discriminant of a quadratic is $b^2 - 4ac$.  % correct: math mode closed with $
			If that discriminant is negative, then there are no real roots.
			
			%	\begin{align*}				% incorrect block: commented out version used invalid math syntax
				%		$x^2-1 &= 0$ \\          % math environment shouldn't use $ inside align
				%		(x+1)(x-1) &=0
				%	\end{align*}
			
			\begin{align*}                           % begins unnumbered aligned equations block
				x^2-1 &= 0 \\                         % first equation
				(x+1)(x-1) &=0                        % second equation
			\end{align*}
			
		\end{document}
